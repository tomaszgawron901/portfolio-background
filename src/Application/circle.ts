
export default class Circle {
    public color: string;
    public radius: number;
    public x: number;
    public y: number;

    public constructor(color: string, radius: number, x: number, y: number ) {
        this.color = color;
        this.radius = radius;
        this.x = x;
        this.y = y;
    }
}